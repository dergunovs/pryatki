import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dergunov.pryatki',
  appName: 'Спрячься от Даши!',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
